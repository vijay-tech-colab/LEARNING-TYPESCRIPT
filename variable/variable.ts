// ypeScript allows you to specify the type of a variable using a colon : followed by the type.

let age: number = 25;        // Variable with a number type
const name1: string = "John"; // Constant variable with a string type
console.log(age);
console.log(name1);